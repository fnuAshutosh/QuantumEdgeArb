# API Contracts

This document repeats the request/response shapes defined in
`DATA_SCHEMA.md` for quick reference.  All HTTP services and Kafka producers
must produce and consume exactly these JSON objects.

See `ml_model/deploy/api_server.py` for FastAPI implementation.
Refer back to `DATA_SCHEMA.md` for extended explanation and example values.

*(The contents are identical to the sections marked 5.x and 7 above.)*
